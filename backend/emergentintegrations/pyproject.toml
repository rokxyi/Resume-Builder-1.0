[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "llm-integrations"
version = "0.1.0"
description = "LLM integration interface for Resume Builder"
requires-python = ">=3.8"
